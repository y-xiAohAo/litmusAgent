# 归档（Human 视角）— 联调深挖：三项调查 + EVAL-010 + S4p 实验

> 生成：2026-07-18 | 模式：thematic | 受众：human
> Source Index：
> - `mydocs/specs/2026-07-18_02-50_real-llm-e2e-suite.md`（Change Log 全程记录）
> - `docs/evaluation-log.md`（EVAL-010、S4p 记录）
> - 真实 Docker 实验、DeepSeek API 复现实验、S4×8 / S4p×3 对照实验原始输出

---

## 1. 背景

真实联调（5/7 PASS）后，对两个 FAIL 场景（S2/S4）做消息级深挖，衍生三项独立调查与一个关键 bug 修复。

## 2. 三项调查结论

### 调查 1：S2 权限 Bug（EVAL-010）溯源

| 问题 | 答案 |
|---|---|
| 何时引入 | TD-001（`ff2777a`，2026-07-16）workspace volume 挂载 /workspace（root 属主）× Phase 3.4 容器 nobody 用户 = 权限不对称 |
| 为何疏忽 | 三重盲区：① TD-001 开发期 Docker 不可用，零真实验证；② 61 个 mock 测试无法表达 POSIX uid/gid 语义；③ put_file/get_file 同为 daemon root 操作，自洽掩盖 |
| 修复 | 容器创建后 chown 65534 + put_file tar 条目带 nobody uid/gid（~20 行，真实回归 3/3） |

### 调查 2：DSML 文本 tool call——模型怪癖还是我方问题

完整复刻 S2 出错上下文 ×5 调 DeepSeek API：**0/5 复现**，全部返回正确的结构化 tool_calls。结论：**模型侧低频格式失败**，我方消息序列化（标准 OpenAI 格式）与错误文案无缺陷。

### 调查 3：S4 工具"方差"——框架不稳还是模型限制

S4 ×8（含 T=0×3）：**8/8 跳过 file_edit 与改标题**——非方差，是系统性模型行为：① 已知全文时偏好 file_write 全量重写（理性但不符合场景要求）；② v4-flash 多步指令静默丢步骤。框架确定性执行无缺陷。

## 3. S4p 对照实验（本轮最高价值产出）

| 组 | file_edit | 标题正确 | 计划步骤 |
|---|---|---|---|
| S4（裸 prompt） | 0/8 | 0/8 | — |
| S4p（TaskPlan 4 步） | **3/3** | **3/3** | **12/12** |

**结论：框架 Planner 机制可完全补偿模型的多步指令弱点**——不是让模型变聪明，而是每轮把"你在哪里、该干什么"外置到 system prompt。

## 4. 连带修复

- **EVAL-010**（高）：workspace 权限对齐（见调查 1），提交 `3b2599e`
- **Planner 进度显示 bug**：计划完成后显示 "Step 5/4"，修复为封顶 + ACTIVE 状态判断，提交 `45abd55`

## 5. 汇报口径（可直接引用）

> 对联调失败场景做消息级溯源，区分出三类根因：框架真实 bug（workspace 权限不对称，TD-001 引入）、模型侧低频怪癖（DSML 格式泄漏，0/5 复现排除我方责任）、系统性模型行为（多步指令丢失）。设计对照实验证明框架规划机制可将多步任务成功率从 0/8 提升至 3/3，为"框架补偿模型弱点"提供量化证据。

## 6. 下一步钩子

- 多步任务默认启用 Planner（优化单元候选）
- DSML 检测重试（小改进，非必须）
- TD-010 网络策略增强 / Web UI 确认单元
